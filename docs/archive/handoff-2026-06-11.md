# Handoff — 2026-06-11

## Session summary

Reviewed open PRs on `celstnblacc/token-diet`, then built and shipped a
server-side path-leak guard and ran a fleet-wide leak audit across `~/DevOpsSec`.

## What shipped

- **PR #68** (external contributor, Jacques-Philippe) merged — replaced hardcoded
  Windows home paths under the previous author's profile in `.vscode/mcp.json`
  with bare `uvx`/`tilth` commands; added `CONTRIBUTING.md`. Closed issue #67.
- **PR #69** merged — new **Path Leak Guard** (`.github/workflows/path-leak.yml` +
  `.github/scripts/path-leak-scan.sh`). Scans PR diff added-lines for hardcoded
  home paths; `on: pull_request`, read-only, no secrets. Job named explicitly so
  the check reports as `Path Leak Guard`.
- **token-diet `main` branch protection:** required status check `Path Leak Guard`
  (admin-bypass allowed, `enforce_admins: false`).
- **token-diet:** secret-scanning + Push Protection enabled.
- **Global hook fix:** `~/.githooks/pre-commit` check 1d Windows pattern made
  JSON-double-escape aware (the `\\+` form catches `C:\\Users\\…` style paths
  the previous single-backslash regex missed — that's how the external
  contributor's home path slipped past). Byte-verified on BSD + GNU grep.

## Why the leak happened (root cause)

Local pre-commit hooks never run on (a) external fork PRs or (b) API-side
`gh pr merge`. The path-leak guard is the server-side mirror that closes both.

## Docs written (in ~/DevOpsSec)

- `GUIDE-data-locality.md` — what leaves the machine vs stays; 5 defense layers.
- `RUNBOOK-leak-audit.md` — repeatable audit procedure (commands).
- `AUDIT-leak-exposure.md` — fleet audit table + triage + recommended actions.
- `token-diet/docs/GUIDE-required-status-checks.md` — how required checks enforce.

## token-diet history decision

History contains prior-author home paths and the operator's local path in old
`.vscode/mcp.json` and docs. **Decision: leave it** — usernames/paths only, no
secrets; rewriting public history force-pushes `main` (operator-only) and is
incompletely effective (forks + orphaned SHAs). Live tree is clean.

## Open follow-ups (fleet audit, not yet done)

1. **`.superharness/` gitignore + `git rm --cached`** across public repos — real
   machine-state leak (`superharness`, `rtk` commit runtime files with
   absolute home paths under the operator's profile). Highest-signal fix.
2. **Enable Push Protection** on public repos where off: superharness,
   obsidian-semantic-mcp, NemoClaw, pr-agent, pencil-sync, autoresearch,
   youtube-mcp.
3. **Fan out path-leak guard** to other public repos (extract to a `gh-guards`
   reusable workflow first — parked until token-diet proves out on a real fork PR).
4. **Stale remotes** (gh 404): always-on-agent, business_with_ai, maf-medical-agents.

## Loose ends

- Local branch `fix/path-leak-guard` still exists (squash-merged; needs `-D`).
- `rtk`/`serena` high live-leak counts are upstream fork noise, not actionable.
